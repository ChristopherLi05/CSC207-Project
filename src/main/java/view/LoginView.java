package view;

import app.IApp;
import interface_adapter.ViewState;

public class LoginView extends AbstractPanel<Object> {
    public LoginView(ViewState<Object> viewState) {
        super(viewState);
    }
}
